var colors = [
    'red',
    'white',
    'blue'
];

console.log(colors[0]);

colors[2] = 'green';
console.log(colors);

var lastIndex = colors.length - 1;
var lastValue = colors[lastIndex];

console.log(lastValue);

